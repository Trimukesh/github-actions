name: Hello

on: [push, pull_request]
jobs:
 test:
   name: javaprogram
   runs-on: self-hosted
   steps:
      - uses: actions/checkout@v1
      - name: setup jdk
        uses: actions/setup-java@v1
        with:
           java-version: 1.8
      - name: setup java
        run: |
           usage: javac trimukesh.java
           
